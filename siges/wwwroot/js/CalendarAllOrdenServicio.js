$(document).ready(function(){
  /*var cal = $('#calendarioAllOs').Calendar({
    locale: 'es',
    weekday: {
      timeline: {
        intervalMinutes: 30,
        fromHour: 1,
        toHour: 24,
        autoResize: true
      },
      dayline: {
        //format: 'MM-DD-YYYY'
      }
    },
    daynotes: diasNotas,
    now: {
      enable: true,
      refresh: true
    }
});*/

  $('#calendarioAllOs').on('Calendar.init', function(event, instance, before, current, after){
  });

  //document.querySelectorAll('i').forEach(function(item){
  //if(item.getAttribute('class').slice(item.getAttribute('class').indexOf('left'), item.getAttribute('class').lenght) == 'left'){
  //item.setAttribute('data-feather','arrow-left-circle');
  //} else if (item.getAttribute('class').slice(item.getAttribute('class').indexOf('right'), item.getAttribute('class').lenght) == 'right') {
  //item.setAttribute('data-feather','arrow-right-circle');
  //}
  //});
  //
  //$('.btn-move-calendar').on('click', function(){
  //document.querySelectorAll('i').forEach(function(item){
  //if(item.getAttribute('class').slice(item.getAttribute('class').indexOf('left'), item.getAttribute('class').lenght) == 'left'){
  //item.setAttribute('data-feather','arrow-left-circle');
  //} else if (item.getAttribute('class').slice(item.getAttribute('class').indexOf('right'), item.getAttribute('class').lenght) == 'right') {
  //item.setAttribute('data-feather','arrow-right-circle');
  //}
  //});
  //feather.replace();
  //});

  //feather.replace();
});

