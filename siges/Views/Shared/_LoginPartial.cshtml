@using Microsoft.AspNetCore.Identity
@using siges.Areas.Identity.Data;
@inject SignInManager<RoatechIdentityUser> SignInManager
@inject UserManager<RoatechIdentityUser> UserManager

@*<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a  class="nav-link" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item align-self-center">
        <a class="nav-link" asp-area="Identity" asp-page="/Account/Register">Registrarme</a>
    </li>
    <li class="nav-item align-self-center">
        <a class="nav-link" asp-area="Identity" asp-page="/Account/Login"><button type="button" class="btn btn-success">Entrar</button></a>
    </li>
}
</ul> *@

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User)){
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="btn btn-danger">Cerrar Sesión</button>
        </form>
    </li>
}
else{
    <li class="nav-item align-self-center">
        @* <a class="btn btn-link" asp-area="Identity" asp-page="/Account/Register">Registrarme</a> *@
    </li>
    <li class="nav-item align-self-center">
        <a class="nav-link" asp-area="Identity" asp-page="/Account/Login"><button type="button" class="btn btn-success">Entrar</button></a>
    </li>
}
</ul> 
